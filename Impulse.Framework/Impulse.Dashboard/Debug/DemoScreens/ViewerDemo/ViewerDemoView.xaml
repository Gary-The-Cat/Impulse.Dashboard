<UserControl x:Class="Impulse.Dashboard.Debug.DemoScreens.ViewerDemo.ResidentialView.ViewerDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Impulse.Dashboard.Debug.DemoScreens.ViewerDemo.ResidentialView"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="{DynamicResource BackgroundBrush}">

    <!-- Caliburn use name pattern matching to add views, the view to be inserted here (by caliburn) will be ViewerView -->
    <Grid>
        <ContentControl 
            Grid.Row="1"
        Background="Red"
        x:Name="ViewerViewModel"/>
    </Grid>
</UserControl>
